# kOps is driven from the control host (your laptop / CI runner), so everything
# runs on localhost with the AWS + kOps CLIs. No SSH inventory needed for create.
[control]
localhost ansible_connection=local

[dev]
localhost ansible_connection=local
